// Stub Jest pour le package `server-only` (Next.js).
//
// `server-only` est un marqueur runtime de Next.js qui throw si importé côté
// client. En environnement Jest (node), on veut juste l'ignorer : le contenu
// du module est vide et n'a aucun comportement à simuler.
module.exports = {};
